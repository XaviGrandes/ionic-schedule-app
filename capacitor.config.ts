import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Restauran_xavi',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
